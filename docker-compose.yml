services:
  data-collection:
    build: .
    environment:
      - RIOT_API_KEY=${RIOT_API_KEY}
      - GOOGLE_CLOUD_PROJECT=${GOOGLE_CLOUD_PROJECT}
      - GOOGLE_APPLICATION_CREDENTIALS=/tmp/key.json
    volumes:
      - .:/app
      - ./service-account.json:/tmp/key.json:ro